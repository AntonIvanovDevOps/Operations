name: Check remote host and push report

on:
  push:
    branches:
    - main
   

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Run hello.js script
        run: node ~/Operations/hello.js